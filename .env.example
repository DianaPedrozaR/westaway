# prisma/schema.prisma targets Postgres. For local dev, point this at a
# reachable Postgres instance (e.g. the Railway plugin's public connection
# string, or a local `docker run postgres`).
DATABASE_URL=""

# Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=""

# Shared passcode gating access to all apps once deployed.
ACCESS_PASSCODE="westaway2026"

# Calendly links for the Intake Concierge's two reps. No real links exist yet
# — the widget shows a graceful fallback message until these are set.
CALENDLY_KYLE_URL=""
CALENDLY_STEPHANIE_URL=""
