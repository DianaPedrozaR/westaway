export const GATE_COOKIE = "wa_gate";

// Value stored in the cookie once the passcode is verified. Not a secret in
// itself — the secret is the passcode required to obtain it.
export const GATE_COOKIE_VALUE = "granted";
